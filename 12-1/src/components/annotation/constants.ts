export const InitialLabelingData = {
  surveyAChoices: [],
  surveyLogic: [],
} as { [key: string]: any };

export const InitialCount = {
  surveyAChoices: 0,
  surveyLogic: 0,
} as { [key: string]: any };
